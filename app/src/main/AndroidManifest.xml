<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gametranslator">

    <!--
        الصلاحيات — الحد الأدنى فقط:
        1. INTERNET               — Google Translate (HTTPS فقط، لا مفتاح API)
        2. SYSTEM_ALERT_WINDOW    — الزر واللوحة العائمة فوق التطبيقات
        3. FOREGROUND_SERVICE     — إبقاء الخدمة في الخلفية
        4. FOREGROUND_SERVICE_SPECIAL_USE — مطلوبة Android 14+

        ❌ بدون VIBRATE
        ❌ بدون RECEIVE_BOOT_COMPLETED
        ❌ بدون READ/WRITE_STORAGE
        ❌ بدون CAMERA أو MICROPHONE
        ❌ بدون مفاتيح API في الكود
        ❌ usesCleartextTraffic محذوفة (HTTPS only)
        ❌ allowBackup=false (لا نسخ احتياطي للبيانات)
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Android 13+ — مطلوب لإظهار إشعار الخدمة -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameTranslator">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".FloatingTranslatorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Overlay translation for games and apps" />
        </service>

    </application>
</manifest>
