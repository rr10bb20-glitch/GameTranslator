<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        الصلاحيات المطلوبة:
        1. INTERNET               — Google Translate API
        2. SYSTEM_ALERT_WINDOW    — الزر والـ Overlay العائم فوق الألعاب
        3. FOREGROUND_SERVICE     — إبقاء الخدمة في الخلفية
        4. FOREGROUND_SERVICE_MEDIA_PROJECTION — مطلوب Android 14+ لـ MediaProjection
        5. POST_NOTIFICATIONS     — إشعار الخدمة (Android 13+)
        6. WAKE_LOCK              — منع النوم أثناء OCR
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameTranslator">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            foregroundServiceType="mediaProjection" — مطلوب حتماً لـ Android 10+
            بدونه يرفض النظام createVirtualDisplay() ويطير MediaProjection
        -->
        <service
            android:name=".FloatingTranslatorService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

    </application>
</manifest>
